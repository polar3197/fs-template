
This is a base template for a full stack web app using PostgreSQL, 
FastAPI and React. 


It runs the server and database in Docker containers with docker compose
and runs the React frontend using Node and Vite.


Setup
1. run docker compose up --build in the root directory
2. update the database config in .env
3. 
# fs-template
